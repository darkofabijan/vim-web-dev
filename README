Installation:

    git clone git@github.com:vlasar/vim-web-dev.git ~/.vim

Create symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc

In vim run :BundleInstall! to install the plugins.

The majority of the .vimrc code taken from https://github.com/jelera
